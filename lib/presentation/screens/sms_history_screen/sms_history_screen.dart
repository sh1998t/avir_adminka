import 'package:flutter/material.dart';

class SmsHistoryScreen extends StatelessWidget {
  const SmsHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SMS History Screen'),
      ),
    );
  }
}
