import 'package:flutter/material.dart';

class StructuresScreen extends StatelessWidget {
  const StructuresScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Structures Screen'),
      ),
    );
  }
}
