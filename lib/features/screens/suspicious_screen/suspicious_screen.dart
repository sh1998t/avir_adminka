import 'package:flutter/material.dart';

class SuspiciousScreen extends StatelessWidget {
  const SuspiciousScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Suspicious Screen'),
      ),
    );
  }
}
