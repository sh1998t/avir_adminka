import 'package:flutter/material.dart';

class ClosingTheDayScreen extends StatelessWidget {
  const ClosingTheDayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Closing The Day Screen'),
      ),
    );
  }
}
