import 'package:flutter/material.dart';

class OperationRegisterScreen extends StatelessWidget {
  const OperationRegisterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Operation Register Screen'),
      ),
    );
  }
}
